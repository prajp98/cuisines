server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/worldbite
    username: root
    password: qwerty
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    validate-on-migrate: true
    locations: classpath:db.migration
  main:
    allow-bean-definition-overriding: true